# Groq API key (for Vercel AI SDK — used by /api/parse-jobs)
GROQ_API_KEY=

# SMTP configuration (Gmail example: use an App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
